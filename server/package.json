{
  "name": "recovery-companion-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend proxy for Recovery Companion — holds the Anthropic API key server-side and forwards chat to Claude.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "@supabase/supabase-js": "^2.106.2",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.0.0",
    "stripe": "^22.2.0"
  }
}
